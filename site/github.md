repo: F-major7/mmm-sandbox
branch: main

## Last sync
date: 2026-09-11

### Updated in this project
- `mmm-data.js` is now GENERATED by `scripts/export_site_data.py` in the repo from `artifacts/posterior.nc`, `data/*.csv` and `data/true_params.json`. Do not edit it by hand; re-run the script after a refit and re-upload.
- `MMM Sandbox.dc.html` binds every number in its prose to `window.MMM`, so a re-export updates the page end to end. Source of truth for the page is `site/index.dc.html` in the repo.
- Fixed: max spend correlation (0.33 -> 0.16), the P10 allocation (real optimizer output, with its lift range), mean sales, the sales range, the seasonality peak, the channel share table (model estimate and generator truth shown side by side), and the model-fit band (now the real per-week 94% predictive interval).
- Removed: `fit.reconR2`, `fit.reconRmse`, `fit.band` (not computed by the project; never displayed or mislabelled).
- Added: plain-English / technical reading mode, a "what this project is" strip, a one-sentence budget recommendation, real posterior response curves with a max-observed-spend marker, and a provenance line in the footer.

## Screen map
| Screen | Repo files |
| --- | --- |
| Hero, intro strip | site/mmm-data.js (generated) |
| 01 Data | data/synthetic_weekly.csv, data/true_components.csv, mmm_sandbox/analysis.py |
| 02 Transforms | mmm_sandbox/transforms.py (same equations in JS) |
| 03 Model fit | artifacts/parameter_recovery.csv, artifacts/diagnostics.json, mmm_sandbox/analysis.fit_summary |
| 04 ROI | mmm_sandbox/analysis.py (channel_roi, marginal_roi, response_curve) |
| 05 Optimizer | mmm_sandbox/optimizer.py |
| Footer | scripts/export_site_data.py (provenance) |
